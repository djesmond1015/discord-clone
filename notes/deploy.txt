1. Set up Virtual Private Server (VPS).
- Log in to Digital Ocean.
- Create project.
- Spin up droplet.

2. Self host Next JS.
- Install Docker.
- Copy and paste Docker Image file with Dockerfile as name.
- git push 

3. Enable reserved IP.

4. ssh root@157.230.194.86 (IP address) to connect VPS.

5. generate ssh key to connect github and VPS from Digital Ocean.
- ssh-keygen -t rsa -b 8192

6. Copy ssh key public file to GitHub.
- cat ~/.ssh/id_rsa.pub  
- ls ~/.ssh/*.pub (list all the ssh key)